type Transfer @entity(immutable: true) {
  id: ID!
  from: Bytes! # address
  to: Bytes! # address
  tokenId: [BigInt!]! # uint256
  value: BigInt
  marketplace: Bytes!
  taker: Bytes
  txHash: Bytes!
}
